name: api
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
# author: Aleksey Danilevskiy <email@example.com>

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  rest_api_server:
    git:
      url: https://github.com/alexd1971/rest_api_server.git
      ref: master
  models:
    path: ../models

dev_dependencies:
  test: ^1.5.1
